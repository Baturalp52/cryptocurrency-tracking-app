# API Configuration

# Client-side API URL (for browser access)
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# Server-side API URL (for Next.js server in Docker)
# Only needed when running in Docker
SERVER_API_URL=http://backend:80/api